# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Backend
APP_ENV=dev
APP_PORT=8000
CORS_ORIGINS=http://localhost:3000

# AI runtime: mock keeps the project runnable without credentials.
# Use any OpenAI-compatible endpoint when ready (OpenAI, Ollama, vLLM, etc.).
LLM_PROVIDER=mock
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
LLM_TIMEOUT_SECONDS=120
